import { ResendToken } from "../components/ui/ResendMail";

const Resend = () => {
   
    return (
        <div className="flex h-screen items-center justify-center">
            <ResendToken />
        </div>
    );
};

export default Resend;